Conway's Game of Life web application built in Java and JS

Dev notes:
2024-11-25
- Initial commit
    - Basic backend (server with toggle cell, random setup, next generation and reset endpoints)
    - Basic frontend (50x50 static grid, buttons, simple styling)